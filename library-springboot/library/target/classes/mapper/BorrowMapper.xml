<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiu.library.mapper.BorrowMapper">

    <insert id="save">
        insert into borrow(book_name,book_no,user_id,user_no,user_name,user_phone,score,days,return_date)
        values(#{bookName},#{bookNo},#{userId},#{userNo},#{userName},#{userPhone},#{score},#{days},#{returnDate})
    </insert>

    <update id="updateById">
        update borrow set book_name = #{bookName},book_no = #{bookNo},user_id = #{userId},user_no = #{userNo},
        user_name = #{userName},user_phone = #{userPhone},score = #{score},status = #{status},days = #{days},
        return_date = #{returnDate},updatetime = #{updatetime}
        where id = #{id}
    </update>

    <update id="deleteById">
        update borrow set isdeleted = 1
        where id = #{id}
    </update>

    <update id="updateStatusById">
        update borrow set status = #{status} where id = #{id}
    </update>

    <select id="SelectAllBorrow" resultType="com.qiu.library.entity.Borrow">
        select * from borrow where isdeleted = 0
    </select>

    <select id="listByCondition" resultType="com.qiu.library.common.vo.BorrowPageVo">
        select * from borrow
        <where>
            <if test="isdeleted != null">
                isdeleted = 0
            </if>
            <if test="userName != null and userName != ''">
               and user_name like concat('%',#{userName},'%')
            </if>
            <if test="bookNo != null and bookNo != ''">
                and book_no like concat('%',#{bookNo},'%')
            </if>
            <if test="bookName != null and bookName != ''">
                and book_name like concat('%',#{bookName},'%')
            </if>
        </where>
        order by id desc
    </select>

    <select id="getById" resultType="com.qiu.library.entity.Borrow">
        select * from borrow where id = #{id}
    </select>
    <select id="getCountByTimeRange" resultType="com.qiu.library.common.dto.BorrowReturCountDTO">
        select DATE_FORMAT(createtime,'%Y-%m-%d') as date,count(*) as count from borrow
        where
        <choose>
            <when test="timeRange == 'week'">
                createtime >= DATE_SUB(NOW(), INTERVAL 1 WEEK)
            </when>
            <when test="timeRange == 'month'">
                createtime >= DATE_SUB(NOW(), INTERVAL 1 MONTH)
            </when>
            <when test="timeRange == 'month2'">
                createtime >= DATE_SUB(NOW(), INTERVAL 1 MONTH)
            </when>
            <otherwise>
                createtime > now()
            </otherwise>
        </choose>
        group by date
    </select>
</mapper>